name: "Stage 1c: ShellCheck"
on:
  workflow_call:
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck Scripts Validation
        uses: ludeeus/action-shellcheck@master
